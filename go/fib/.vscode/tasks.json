{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "letsroll",
      "command": "go",
      "args": ["run", "${file}", "10"],
      "options": {
        "cwd": "${workspaceRoot}"
      },
      "group": {
        "kind": "build",
        "isDefault": true
      }
    }
  ]
}
